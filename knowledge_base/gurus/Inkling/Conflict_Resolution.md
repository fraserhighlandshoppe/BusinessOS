# Inkling - Conflict Resolution

Tags: #inkling #conflict #resolution #pagan #cardone #singer #dubeau

References: `MarketingHowTo.md` (`Section 1b`); `SalesHowTo.md` (`Section 1b`); `BusinessHowTo.md` (`Section 7`)

---

## Source Documents
`MarketingHowTo.md` (`Pagan`/`Cardone`/`Hopkins`/`Singer`/`Dubeau`/`Lindenbaum`/`White`/`DigitalMarketer`/`Mirasee`); `SalesHowTo.md` (`Cardone`/`Camp`/`Ziglar`); `BusinessHowTo.md` (`Pagan`/`Cardone`/`Camp`/`Singer`/`Dubeau`/`White`/`Lindenbaum`); `ITHowTo.md` (`Wiki`/`Guru`); `SocialMediaHowTo.md`/`WebConversionHowTo.md`/`MindsetHowTo.md`/`OperationsHowTo.md`/`TradingHowTo.md` (`Section 1b` expanded).

## Key Patterns
- Common agreement paragraph + separate guru paragraphs (`Pagan`: `understand customer`/`trigger action`/`everything is test`; `Cardone`: `under-promoting`/`omnipresence`/`spend aggressively`/`speed`) + recommendation (`Pagan` first `90` days → `Cardone` scale + `omnipresence`).
- `Sales`: `Camp` (`structured options`) + `Cardone` (`high frequency`) + `Ziglar` (`agreement`) → recommendation (`Camp` `negotiation`/`complex`; `Cardone` `prospecting`/`urgency`/`speed`; `Ziglar` `agreement`/`closing`).
- `Brand`: `Singer` (`3 adjectives`/`narrow`) + `Cardone` (`omnipresence`/`broad`) → `3-adjective promise` delivered `omnipresent`.
- `IT`: `Wiki` (`authoritative SOP`) + `Guru` (`no direct coverage`) → `document` + `test restore` + `reference source`.
